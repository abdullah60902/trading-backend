# Render Environment Configuration

This file is deployed on Render using render.yaml

## Email Configuration (Resend)
- RESEND_API_KEY: Configured in render.yaml
- EMAIL_HOST: smtp.gmail.com (fallback)

## Deployment
Redeploy this commit to apply environment variables from render.yaml
